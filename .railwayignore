# Ativos pesados usados só no pitch/validação, não lidos pelo backend em runtime
prototipo/
docs/*.pdf
docs/*.docx
dados_inmet_processado/
dados_inmet_raw/

# Streamlit (só o redirect via_leite_app.py; não faz parte da API)
.streamlit/

# Frontend (deploy separado na Vercel)
frontend/

# Dev / testes locais
tests/
__pycache__/
*.pyc
